services:
  cams-web:
    build: .
    ports:
      - "3001:3001"
    restart: unless-stopped
    container_name: cams-web
    environment:
      - NODE_ENV=production
    networks:
      - cams-network

networks:
  cams-network:
    driver: bridge